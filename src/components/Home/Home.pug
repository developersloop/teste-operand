.container-users.column.justify-center
  .column.wrap.full
    v-container
      .row.no-wrap.justify-end(style="margin: 8px")
        v-btn(
          class="mx-2"
          fab
          dark
          small
          @click="addItem"
        )
          v-icon(
            dark
          ) mdi-plus
      v-card
        v-card-title
           v-text-field(
              v-model="search"
              append-icon="mdi-magnify"
              label="Search"
              single-line
              hide-details 
            ) 
                  
        v-data-table(
          :headers="headers"
          :items="items"
          :search="search"
          :items-per-page="10"
          class="elevation-1"
          loading
          loading-text="Carregando..."
        )

          template(v-slot:item.actions="{ item }")
            .row.text-center.justify-center
              v-icon(
                style="flex-grow: 0.06"
                small
                class="mr-2"
                @click="editItem(item)"
              ) mdi-pencil
              
              v-icon(
                small
                @click="deleteItem(item)"
              ) mdi-delete



  // editar usuário
  dialog-edit-add-user(
    :dialogStatus="showDialogAddEditUser"
    :item="item"
    @save="saveItem"
    @closeDialog="showDialogAddEditUser = false"
  )

  // remover usuário
  dialog-delete-confirmation(
    :dialogStatus="showDialogDeleteUser"
    :item="item"
    @confirm="removeItem"
    @closeDialog="showDialogDeleteUser = false"
  )
    template(slot="title") Exclusão
    template(slot="text") Deseja remover este Usuário?